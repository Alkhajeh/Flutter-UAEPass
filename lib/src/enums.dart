/// Used to set environment
enum UaePassEnv { stg, prod }

/// Used to set language
enum UaepassLang { en, ar }

/// Used to set button type
enum UaepassButtonType { normal, pill, sharp }

/// Used to set button theme
enum UaePassButtonTheme { outline, white, dark }
